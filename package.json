{
  "name": "@bitsage/mcp-server",
  "version": "0.1.0",
  "description": "MCP Server for BitSage Network - Enable LLMs to interact with distributed compute",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "bitsage-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "starknet": "^6.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "mcp",
    "bitsage",
    "llm",
    "ai",
    "distributed-compute",
    "starknet"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Bitsage-Network/bitsage-mcp"
  }
}
